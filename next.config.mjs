/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        remotePatterns: [
            {
                hostname: "outstanding-mastiff-966.convex.cloud" ,

            },
        ],
    },
};

export default nextConfig;
